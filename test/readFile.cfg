process TEST = {
	source = DaqSource{ string reader = "CSCFileReader"
        	untracked int32 maxEvents = 50000
		PSet pset = { untracked vstring fileNames = {'input1.raw','input2.raw'} }
	}

	module out = PoolOutputModule {
        	untracked string fileName ="rawdata.root"
	}

	path p = {out}
}
