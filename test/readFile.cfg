process TEST = {
	source = DaqSource{ string reader = "CSCFileReader"
		untracked int32 maxEvents = 10
		PSet pset = {
			untracked vstring RUI00 = {
				'/tmp/kkotov/csc_00000555_EmuRUI02_Monitor_000.raw'
			}
			untracked vstring FED750 = {'RUI00'}
			untracked vstring FED760 = {'RUI02'}
			untracked int32 firstEvent = 0
		}
	}

	module out = PoolOutputModule {
        	untracked string fileName ="/tmp/kkotov/tmp.root"
	}

	endpath e = {out}
}
